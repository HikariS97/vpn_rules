---
name: latex-paper-review
description: 辅助学术论文的 LaTeX 写作、检查、润色和审阅，使用编号式修改标注流程。适用于用户要求对 LaTeX 论文进行语病修改、逻辑修改、全文一致性检查、缩写检查、时态检查、引用/图表编号格式检查、单位符号检查、版面检查或编译验证，并希望保留逐条确认/拒绝流程的场景。
---

# LaTeX 论文审阅

## 必要输入

开始编辑前，先确定或询问：

- LaTeX 项目目录。
- 主 `.tex` 文件。
- 当前任务类型，例如逻辑、语病、术语一致性、缩写、时态、格式、版面等。
- 本篇论文的特定偏好。

不要在 skill 中写死项目路径、文件名、缩写排除项、单位偏好、引用格式偏好或符号偏好。这些都应作为每篇论文或每次会话的配置。

## 审阅标注流程

使用编号式修改，方便用户逐条确认或拒绝。

默认宏：

```tex
\newcommand{\revnum}[1]{\textsuperscript{\textbf{[#1]}}}
\newcommand{\revadd}[1]{\textcolor{blue}{#1}}
\newcommand{\revdel}[1]{\textcolor{red}{\sout{#1}}}
\newcommand{\revok}[1]{\textcolor{green!50!black}{#1}}
```

每一处建议修改使用：

```tex
\revnum{N}\revdel{原文}\revadd{修改后文字}
```

当用户确认或拒绝某处修改后，把最终保留文字改成绿色，并保留编号：

```tex
\revnum{N}\revok{最终保留文字}
```

当用户再次确认绿色文字无误后，去掉 `\revnum` 和 `\revok`，恢复普通正文。

继续使用下一个可用编号，不要重用已有编号。

## 批次规则

每一批修改只能处理一种类型的问题，例如：

- 段落逻辑。
- 语病和表达流畅性。
- 术语一致性。
- 缩写处理。
- 时态。
- 引用和交叉引用格式。
- 单位和符号。
- LaTeX 空格、缩进或版面。

除非用户明确要求，不要在同一批里混合多种类型的修改。

## 编辑原则

保守编辑，保留作者原意。

优先做局部修改，不要轻易大段重写。除非用户要求压缩或深度润色，不要削弱、简化或改变技术表述。

不要静默改变记号、单位、citation 合并方式、图表引用风格或缩写习惯。如果这类修改可能影响含义或风格，应先询问用户，或作为单独编号建议提出。

## 缩写

遇到新的缩写时：

1. 检查全文中是否已经给出全称。
2. 如果没有给出全称，建议在首次出现处补充。
3. 如果该缩写可能足够常见、可以不展开，先询问用户是否加入本篇论文的排除列表。
4. 排除列表只对当前论文或当前会话生效，除非用户明确要求长期保存。

不要在 skill 中写死 `RGB`、`GPU`、`LiDAR`、`MDE` 等排除项。

## 全文一致性检查

检查应覆盖全文，而不是只看当前段落。重点包括：

- 术语和大小写。
- 连字符和复合词。
- Fig.、Table、Sec.、Eq.、Algorithm 等引用风格。
- citation 空格、标点和分组方式。
- 单位和符号。
- 缩写首次定义。
- 时态。

如果全文已有稳定习惯，优先跟随已有习惯。如果文中出现多种写法，提出统一方案，并作为单独批次修改。

## IEEE 参考文献与 BibTeX

参考文献和 BibTeX 检查应作为单独批次执行。对于 IEEE 稿件，必须以论文所用 IEEE 模板及其 `IEEEtran.bst` 输出为最终格式标准，不手工拼接或覆盖参考文献样式，也不擅自切换到 `biblatex`、其他 `.bst` 或其他出版机构的格式。

检查或修改 `.bib` 时，应同时核对主 `.tex`、BibTeX 数据和编译后的 `.bbl`/PDF，严格执行以下要求：

- 每个 `\cite{...}` 的键都必须存在且唯一；不得存在重复键、重复文献记录、拼写不一致的键或未解析 citation。
- 根据文献实际类型选择 `@article`、`@inproceedings`、`@book`、`@techreport` 等 entry type，不得为了改变显示效果而使用错误类型。
- 核对作者、标题、期刊或会议名称、年份、卷号、期号、页码、DOI、出版者等元数据。不得猜测、补造或静默改写无法验证的信息；有 DOI 时优先用 DOI 或出版方页面核验。
- 作者之间使用 BibTeX 的 `and` 分隔；机构作者使用额外花括号保护。不要把作者列表手工写成已经排版好的 IEEE 文本。
- 标题遵循 IEEE 的 sentence-style capitalization。仅对必须保留大小写的缩写、专有名词、算法名和数学符号使用花括号保护，例如 `{SLAM}`、`{LiDAR}`、`{P}n{P}`；不要无必要地给整个标题加花括号。
- 页码范围使用双连字符，例如 `123--130`。DOI 字段只保留规范 DOI 标识，不同时在 DOI 和 URL 字段重复写 `https://doi.org/...`。
- 期刊名和会议名应使用一致、可由 `IEEEtran.bst` 正确处理的正式名称或 IEEE 标准缩写；全文不得混用全称、非标准缩写和不同缩写版本。无法确定缩写时先核验，不自行创造缩写。
- 保留模板要求的 `\bibliographystyle{IEEEtran}` 和 `\bibliography{...}` 工作流。修改后必须重新运行 LaTeX--BibTeX--LaTeX--LaTeX 或等效的 `latexmk` 流程。
- 编译后检查参考文献是否按首次引用顺序编号，作者、标题、期刊/会议、卷期、页码、年份和 DOI/URL 的输出是否符合 IEEE 样式，并检查缺失字段、问号、重复编号、未定义 citation 和 BibTeX warning。

仅当用户明确授权时，才删除未引用的 `.bib` 条目或合并疑似重复记录。任何会改变引用所指文献的修改，都必须先提出并取得用户确认。

## 符号和单位

符号和单位选择视论文而定，不写死规则。

不要绝对规定必须使用 `\sim` 而不能使用 `\approx`，也不要绝对规定某个单位必须保留或转换。应当：

- 检查全文是否不一致。
- 简要说明可选方案。
- 询问用户，或提出编号式一致性修改。
- 当单位转换可能影响可读性、惯例或与图表对应关系时，优先保留原单位并征求用户意见。

## 时态

根据语义选择时态：

- 已经完成的实验、本论文中已经做过的操作、过去工作的方法或结果，用过去式。
- 一般事实、本文结构、定义、公式、算法和当前仍成立的论断，用一般现在时。
- 未来计划用将来时。

时态修改应作为单独批次处理。

## LaTeX 安全

尽量做 LaTeX-aware 的修改。

对 `\revdel{...}` 中包含 citation、reference、equation、footnote 或复杂宏的情况要谨慎。必要时拆分修改，避免破坏编译。

修改后使用用户指定的 LaTeX 编译方式。如果用户没有指定，应询问或根据项目判断，不要随意切换编译引擎。

编译时检查：

- 编译错误。
- 未定义引用或 citation。
- overfull box。
- 严重版面问题。
- 用户关心时的页数变化。

## 版面修改

只有当用户明确要求压缩页数或调整版面时，才修改版面。

优先使用最小、可逆的改动。正式修改前，可以在临时副本中测试多个方案。不要未经用户同意就改字体、字号、页边距、图尺寸或主要排版结构。

版面修改必须作为单独类型处理，不要和正文修改混在一起。
