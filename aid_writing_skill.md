---
name: latex-paper-review
description: 辅助学术论文的 LaTeX 写作、检查、润色和审阅，使用编号式修改标注流程。适用于用户要求对 LaTeX 论文进行语病修改、逻辑修改、全文一致性检查、缩写检查、时态检查、引用/图表编号格式检查、单位符号检查、版面检查或编译验证，并希望保留逐条确认/拒绝流程的场景。
---

# LaTeX 论文审阅

## 必要输入

开始编辑前，先从当前文件、项目要求和会话中确定以下信息；只有缺失信息会影响本轮工作时才询问：

- LaTeX 项目目录。
- 主 `.tex` 文件。
- 当前任务类型，例如逻辑、语病、术语一致性、缩写、时态、格式、版面等。
- 本篇论文的特定偏好。

不要在 skill 中写死项目路径、文件名、缩写排除项、单位偏好、引用格式偏好或符号偏好。这些都应作为每篇论文或每次会话的配置。

## 审阅标注流程

默认使用以下编号式修改流程，方便用户逐条确认或拒绝；用户明确指定直接修改、定稿或清理标注时，按其要求执行。

优先复用项目已有的审阅宏。需要新建以下默认宏时，先检查命令是否已定义，并补齐 `xcolor`、`ulem` 依赖；`ulem` 使用 `normalem` 选项以保留原有强调格式，避免重复加载包或定义命令。

```tex
\newcommand{\revnum}[1]{\textsuperscript{\textbf{[#1]}}}
\newcommand{\revadd}[1]{\textcolor{blue}{#1}}
\newcommand{\revdel}[1]{\textcolor{red}{\sout{#1}}}
\newcommand{\revok}[1]{\textcolor{green!50!black}{#1}}
```

每一处建议修改使用：

```tex
\revnum{N}\revdel{原文}\revadd{修改后文字}
```

当用户确认或拒绝某处修改后，把最终保留文字改成绿色，并保留编号：

```tex
\revnum{N}\revok{最终保留文字}
```

当用户再次确认绿色文字无误后，去掉 `\revnum` 和 `\revok`，恢复普通正文。

继续使用下一个可用编号，不要重用已有编号。

续接任务时，先核对实时源码、审阅记录与最新用户决定。区分待确认、绿色待二次确认、已定稿、已拒绝和待技术决策；未另行指定时，按用户确认时已有的状态推进，不因一次笼统的“都确认”让同一项连续越过两步。用户已明确授权的修改直接落实，不重复索取同一授权。

没有新证据时，不重新提出已拒绝或已处理的建议。未处理的技术问题保留原状态；文字定稿不表示技术问题已解决。明确报告已经应用的修改与仅提出的建议。

## 批次规则

默认按问题类型组织修改与报告，例如：

- 段落逻辑。
- 语病和表达流畅性。
- 术语一致性。
- 缩写处理。
- 时态。
- 引用和交叉引用格式。
- 单位和符号。
- LaTeX 空格、缩进或版面。

用户要求专项检查时遵守其范围；明确要求全面检查时，可以一轮覆盖多类问题并集中记录，不必为同类遗漏反复拆轮。

## 编辑原则

- 保守编辑，优先保留作者的句式、语序和已确认措辞。每处改写应解决明确问题；语法正确、含义清楚时，不仅为换一种说法而修改。没有发现新问题时如实说明。
- 精简铺垫、重复修饰和重复定义，同时保留已知/未观测信息、适用条件、比较对象、指标、范围及证据强度。不要把基础条件夸大为核心贡献，也不要因压缩而模糊贡献主次。
- 改写后核对修饰语仍限定原对象；先判断并列、让步、转折和因果关系，再选择连接词。不要无依据地添加 respectively、all、best 等关系或范围词，也不要把可能原因写成已证实的机制。
- 结果和未来工作应说明具体对象、能力或评价范围。指标不同、算法版本不同或改进幅度不同，不能用一个笼统的 accuracy 概括而改变原意。
- 数学形式、定义、数据和算法事实的变动不得混作普通措辞修饰；按本篇已约定的保护范围及用户授权处理。记号、单位、citation 分组或图表风格也应遵循本篇要求，不静默改变。

## 缩写

遇到新的缩写时：

1. 以最终保留的可见文本检查首次出现，排除注释和删除稿；宏定义的位置不算首次出现，但要检查宏在正文调用处展开后的文本。摘要和正文分别核对，兼顾图注与表格。
2. 如果没有给出全称，建议在首次出现处补充。
3. 区分通用缩写、算法专名、单位、型号和数学记号。常见缩写优先沿用本篇已明确的例外；没有明确偏好而确实需要决定时，提出建议，不反复询问已决定的例外。算法专名的全称必须有来源，不猜测。
4. 排除列表只对当前论文或当前会话生效，除非用户明确要求长期保存。

不要在 skill 中写死 `RGB`、`GPU`、`LiDAR`、`MDE` 等排除项。

## 全文一致性检查

进行全文一致性检查时，覆盖全文；专项检查只核对与其范围相关的内容及上下文。重点包括：

- 术语和大小写。
- 连字符和复合词。
- Fig.、Table、Sec.、Eq.、Algorithm 等引用风格。
- citation 空格、标点和分组方式。
- 单位和符号。
- 缩写首次定义。
- 时态。

如果全文已有稳定且合理的习惯，优先跟随；存在不一致时，结合本轮范围提出统一方案。

检查交叉引用时，同时核对引用是否解析、是否指向正在解释的对象，以及图表内容是否支持相关结论。每张图表应在合适位置引用；同一图若能帮助解释不同位置的内容，可以再次引用，不机械地限制为一次或在每次提及时重复。缺失目标单独记录，不猜测图号。

用户更新分组、算法版本、指标或数据后，检查设置、图注、表头、结果段与结论是否同步。只在证据支持的比较对象、指标和设置范围内概括优势。

## 技术说明与证据

进行逻辑或技术检查，或发现具体语义疑点时：

- 按阅读和计算依赖安排定义。主要对象在首次需要的位置交代，辅助量可以在整体公式之后解释；已有记号复用或交叉引用，避免重复引入同一 Jacobian 等对象。一次使用且自然语言足够清楚的概念不必另设符号。
- 反复出现的条件或情形可统一定义，并说明适用范围、边界归属和例外；在声明的推导范围内默认使用，切换情形时再提示。局部假设不能写成未经证实的全局保证。
- 区分精确目标、替代目标和实际近似步骤，用最少必要文字说明近似发生在哪里、保留了什么及依赖的前提；不要以“差不多”代替依据，或把近似写成严格等价。
- 不按表面相似统一数学符号。检查微分与增量、标准差与协方差、真值与估计量、符号与大小、系数与平方后的权重，以及边界处是否仍满足所用推导前提。
- 公式、引用文献、实际实现和实验处理若不一致，单独列出技术问题与证据，不用语言改写掩盖差异。仅在核实具体论断确有必要且材料可用时查看调用链、配置和保存结果；区分初始化与 refinement 后结果、聚合规则和失败样本处理。当前代码不自动证明历史图表使用了同一版本，普通润色不要求重跑实验。

## IEEE 参考文献与 BibTeX

按目标出版物的明确要求及适用的 [IEEE Reference Guide](https://ieeeauthorcenter.ieee.org/wp-content/uploads/IEEE-Reference-Guide.pdf) 检查，记录采用的指南版本或核对日期，保留项目已有的 IEEE/BibTeX 工作流。模板的默认输出可能落后于规范或忽略字段，不能仅因使用 `IEEEtran.bst` 就认定格式合规。不手改生成的 `.bbl`，也不擅自切换书目系统或其他出版机构的样式。

需要完整检查时，集中核对全库元数据、正文 citation 和实际生成的参考文献，包括未引用但保留的条目，避免逐轮只挑个别大小写问题。

- 每个 citation 键应唯一解析到一个书目条目，同一键可以在正文多次引用；检查书目中的重复键、重复文献、未解析引用、编号顺序和本篇约定的 citation 分组方式。
- 根据实际类型使用 `@article`、`@inproceedings`、`@book` 等，不为改变显示效果使用错误类型。
- 核对作者顺序、题名、出版物名称、正式日期、卷期、页码或文章号、版次、出版社、出版地点与已有 DOI。优先核对出版方记录和原始出版物，用 Crossref 等交叉验证；来源冲突时核实具体版本，不拼合不同版本的元数据，不猜测缺失值。
- 作者以 BibTeX 的 `and` 分隔，机构作者适当保护；人名以名的首字母加姓输出。保留已核实的完整作者元数据，通过样式支持的控制项实现截断；按适用 IEEE 规则检查六位及以内完整列出、超过六位第一作者加 et al.，特别核对边界值。非 IEEE 来源未提供其余作者姓名时，指南允许使用 et al.，不推测补齐。
- 文章标题使用 sentence case，只保护必要缩写、专名和数学符号；书名按书籍规则使用 Title Case。不要将文章标题的规则套用于书名，也不要整体保护文章标题。
- 期刊及会议名依 `IEEEabrv.bib`、官方缩写表和通则使用经核实的缩写；仅含一个单词的刊名保留全称，无法据此确定缩写的词也保留全称，不自行创造缩写。区分强制字段与可选信息，例如会议地点并非一律必填。
- 正式编入卷期的文章采用卷期出版日期，不误用 online-first、数据库入库或历史文献数字化日期。没有期号或月份的连续出版条目不编造信息。
- 页码范围使用 `--`；文章号使用相应格式，不将文章内部页码当作卷页码。书籍核对版次、出版地点和出版社，不把版次误写成卷期。
- 默认不在参考文献中额外添加 DOI 显示，也不为形式完整而逐条补齐 DOI。已有的纯 DOI 标识保留为 `.bib` 元数据，不因不显示而删除。IEEE 指南的 “DOIs are included, when provided by the author.” 不能解释为所有文献都必须补齐并打印 DOI；缺少 DOI 显示本身不应被一概判为格式错误。
- 只有用户要求或目标出版物明确要求时，才补充 DOI 输出；例如适用 IEEE early access 规则时，应注明状态、在线发表日期与 DOI。区分具体投稿要求、通用指南与作者偏好；已明确不显示 DOI 时，后续检查不要自动恢复。若与已核实的具体要求冲突，说明差异，不擅自覆盖用户决定。
- DOI 元数据与显示分别管理。部分样式版本（如 `IEEEtran.bst` 1.14）不读取 `doi` 字段，不必因此用 `note` 强制打印。确需输出时才使用样式支持的方式，并核查链接、标点及重复；用户要求不显示时，移除 `note` 中用于 DOI 输出的文字和链接，保留文章号等其他出版信息。不要为此修改模板或生成的 `.bbl`，也不要在 URL 字段重复同一 DOI。
- 保留模板的 `\bibliographystyle{...}`、`\bibliography{...}` 工作流及经过确认的控制项。修改后运行等效的完整 LaTeX/BibTeX 编译流程，核查最终作者截断、大小写、名称、卷期、页码、文章号、编号与警告，并确认 DOI 显示符合本篇决定，不能仅凭源字段存在或编译成功判定完成。
- 未引用条目可在临时目录生成完整 BibTeX 输出验证，不为检查而改变正文引用范围。是否提取或渲染 PDF 按任务和项目配置决定。

仅当用户明确授权时，才删除未引用条目、合并记录或改变引用所指文献；已有授权无需重复确认。集中记录已修正项、来源及无法确认的信息，避免将已处理的问题重新提出。

## 符号和单位

符号和单位选择视论文而定，不写死规则。

不要绝对规定必须使用 `\sim` 而不能使用 `\approx`，也不要绝对规定某个单位必须保留或转换。应当：

- 检查全文是否不一致。
- 简要说明可选方案。
- 询问用户，或提出编号式一致性修改。
- 当单位转换可能影响可读性、惯例或与图表对应关系时，优先保留原单位并征求用户意见。

## 时态

根据语义选择时态：

- 已完成的数据采集、实验操作和过去工作通常用过去式；介绍本文实验设置、比较内容及图表呈现的结果时，也可采用与全文一致的现在时，不机械地统一所有实验句子的时态。
- 一般事实、本文结构、定义、公式、算法和当前仍成立的论断，用一般现在时。
- 未来计划用将来时。

时态修改遵循本轮范围，优先保留作者已有且合理的叙述习惯。

## LaTeX 安全

尽量做 LaTeX-aware 的修改。

对 `\revdel{...}` 中包含 citation、reference、equation、footnote 或复杂宏的情况要谨慎。必要时拆分修改，避免破坏编译。

修改前读取实时文件，使用局部编辑或差异核对保留用户并行修改，避免用旧快照覆盖新内容。

修改后沿用用户指定或项目已有的编译方式，不随意切换引擎。同步文件、链接颜色及 PDF 渲染按项目配置处理。只有说明文档或 skill 文本变化、论文源文件未变时，不重复编译论文。

编译时检查：

- 编译错误。
- 未定义引用或 citation。
- overfull box。
- 严重版面问题。
- 用户关心时的页数变化。

编译检查应区分本轮新增问题与原有遗留问题，并确认生成文件对应当前源码；工具成功返回不代表引用、书目及修订宏已正确输出。

## 版面修改

只有当用户明确要求压缩页数或调整版面时，才修改版面。

优先使用最小、可逆的改动。正式修改前，可以在临时副本中测试多个方案。不要未经用户同意就改字体、字号、页边距、图尺寸或主要排版结构。

版面修改必须作为单独类型处理，不要和正文修改混在一起。
